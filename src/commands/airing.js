module.exports = {
    name: 'airing',
    description: 'Sends a list of currently airing shows.',
    args: true,
    needsArgs: true,
    WIP: true,
    run: ({ database, message, args }) => {
        
    }
}